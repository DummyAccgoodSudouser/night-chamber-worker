const JWKS_URL="https://www.googleapis.com/service_accounts/v1/jwk/securetoken@system.gserviceaccount.com";
type Claims={aud?:string;iss?:string;sub?:string;exp?:number;iat?:number;auth_time?:number;name?:string;email?:string;[k:string]:unknown};
let cache:{keys:Record<string,CryptoKey>;until:number}|null=null;
function b64url(s:string){const pad=s.length%4?"=".repeat(4-s.length%4):"";const raw=atob(s.replace(/-/g,"+").replace(/_/g,"/")+pad);return Uint8Array.from(raw,c=>c.charCodeAt(0))}
function json<T>(s:string){return JSON.parse(new TextDecoder().decode(b64url(s))) as T}
async function getKeys(){if(cache&&cache.until>Date.now()+60000)return cache.keys;const r=await fetch(JWKS_URL,{cf:{cacheTtl:3600}});if(!r.ok)throw Error(`Firebase key fetch failed (${r.status}).`);const data=await r.json() as {keys?:Array<JsonWebKey&{kid?:string}>};if(!Array.isArray(data.keys))throw Error("Firebase key set is invalid.");const out:Record<string,CryptoKey>={};for(const k of data.keys){if(!k.kid||k.kty!=="RSA")continue;out[k.kid]=await crypto.subtle.importKey("jwk",k,{name:"RSASSA-PKCS1-v1_5",hash:"SHA-256"},false,["verify"])}cache={keys:out,until:Date.now()+3600000};return out}
export async function verifyIdToken(token:string,project:string){const p=token.split(".");if(p.length!==3)throw Error("Firebase token is malformed.");const h=json<{alg?:string;kid?:string}>(p[0]),c=json<Claims>(p[1]),now=Math.floor(Date.now()/1000);if(h.alg!=="RS256"||!h.kid)throw Error("Unsupported Firebase token.");if(c.aud!==project||c.iss!==`https://securetoken.google.com/${project}`)throw Error("Firebase token belongs to another project.");if(!c.sub||!c.exp||c.exp<=now)throw Error("Firebase session expired. Please log in again.");if(!c.iat||c.iat>now+60)throw Error("Firebase token time is invalid.");const key=(await getKeys())[h.kid];if(!key)throw Error("Firebase signing key not found. Try again.");const ok=await crypto.subtle.verify({name:"RSASSA-PKCS1-v1_5"},key,b64url(p[2]),new TextEncoder().encode(`${p[0]}.${p[1]}`));if(!ok)throw Error("Firebase token signature is invalid.");return{uid:c.sub,claims:c}}
export async function requireAuth(req:Request,project:string){const h=req.headers.get("authorization")||"";if(!h.startsWith("Bearer "))return new Response(JSON.stringify({ok:false,message:"Authentication required."}),{status:401,headers:{"content-type":"application/json"}});try{return await verifyIdToken(h.slice(7),project)}catch(e){return new Response(JSON.stringify({ok:false,message:e instanceof Error?e.message:"Unauthorized"}),{status:401,headers:{"content-type":"application/json"}})}}
